enum Flavor { prod, dev }

enum LoadingProgress { idle, loading, done, failed }

enum OtpType { signup, forgotpassword }

enum AppModule { general, securities, assetManagement, mutualFunds }

enum AppThemeMode { light, dark }

enum WalletAction { addFund, transferFund, withdrawFund }

enum PaymentUrlLauncherCaller { wallet, subscription }

enum MutualFundDocType { deed, prospectus }
